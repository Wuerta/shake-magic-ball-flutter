import 'package:flutter/material.dart';

void main() {
  runApp(ShakeBallMagic());
}

class ShakeBallMagic extends StatelessWidget {
  const ShakeBallMagic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
